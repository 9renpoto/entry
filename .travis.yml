language: node_js
sudo: false
node_js: stable
env:
  matrix:
  - CMD=test
  - CMD=lint
  - CMD=build
script:
- npm run $CMD
after_script:
- '[ "$TRAVIS_BRANCH" == master ] && [ "$TRAVIS_PULL_REQUEST" == "false" ] && ["$CMD" === "test"] && bash ./deploy.sh'
notifications:
  email: false
